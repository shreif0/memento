{
  "name": "@memento/dashboard",
  "version": "0.1.0",
  "description": "Thin control-plane UI for browsing and managing agent sessions, backed by @memento/harness.",
  "type": "module",
  "main": "src/server.ts",
  "license": "UNLICENSED",
  "private": true,
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "start": "node --experimental-strip-types src/server.ts",
    "test": "node --experimental-strip-types --test test/*.test.ts"
  },
  "dependencies": {
    "@memento/harness": "*"
  },
  "devDependencies": {
    "@types/node": "^22",
    "typescript": "^5"
  }
}
